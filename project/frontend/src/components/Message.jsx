import React from "react";
import { Alert } from "react-bootstrap";
const Message = () => {
  return (
    <div>
      <Alert color="danger">There is an error while loading the medicine</Alert>
    </div>
  );
};

export default Message;
